<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.example.gluco">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:noHistory="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Dietas"></activity>
        <activity android:name=".Paciente" />
        <activity android:name=".AgregarDieta" />
        <activity android:name=".registro_usuario" />
        <activity
            android:name=".BarraNavegacion"
            android:label="@string/TituloBarra"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CompletarRegistro" />
        <activity android:name=".Registro_en_app" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>